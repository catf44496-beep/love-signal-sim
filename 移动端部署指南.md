# 移动端部署指南

## 📱 已完成的移动端优化

### 1. 移动端 Meta 标签
- ✅ 添加了 viewport 配置
- ✅ 禁用了用户缩放（防止误操作）
- ✅ 添加了 PWA 支持标签
- ✅ 配置了主题颜色

### 2. 移动端样式优化
- ✅ 创建了 `mobile.css` 移动端专用样式
- ✅ 优化了触摸交互（防止双击缩放）
- ✅ 优化了按钮最小尺寸（44x44px，符合移动端标准）
- ✅ 添加了安全区域适配（iPhone X 等刘海屏）
- ✅ 优化了字体渲染

### 3. PWA 支持
- ✅ 创建了 `manifest.json`
- ✅ 配置了应用图标和主题色
- ✅ 支持添加到主屏幕

## 🚀 部署步骤

### 方法一：使用 Vercel（推荐，免费且快速）

1. **安装 Vercel CLI**（可选，也可以直接用网页版）
   ```bash
   npm i -g vercel
   ```

2. **构建项目**
   ```bash
   npm run build
   ```

3. **部署到 Vercel**
   ```bash
   vercel
   ```
   或者访问 https://vercel.com，连接 GitHub 仓库自动部署

4. **配置自定义域名**（可选）
   - 在 Vercel 项目设置中添加你的域名

### 方法二：使用 Netlify（同样免费）

1. **访问 Netlify**
   - 访问 https://www.netlify.com
   - 连接 GitHub 仓库

2. **配置构建设置**
   - Build command: `npm run build`
   - Publish directory: `dist`

3. **自动部署**
   - Netlify 会自动检测到 Vite 项目并配置

### 方法三：使用 GitHub Pages

1. **安装 gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **修改 package.json**
   ```json
   {
     "scripts": {
       "predeploy": "npm run build",
       "deploy": "gh-pages -d dist"
     },
     "homepage": "https://catf44496-beep.github.io/love-signal-sim"
   }
   ```

3. **部署**
   ```bash
   npm run deploy
   ```

4. **在 GitHub 仓库设置中启用 Pages**
   - Settings → Pages
   - Source: gh-pages branch

### 方法四：使用自己的服务器

1. **构建项目**
   ```bash
   npm run build
   ```

2. **上传 dist 文件夹**
   - 将 `dist` 文件夹内容上传到服务器
   - 配置 Nginx 或其他 Web 服务器

3. **Nginx 配置示例**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       root /path/to/dist;
       index index.html;

       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```

## 📲 手机访问测试

### 本地测试（同一 WiFi）

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **查看本机 IP**
   - Windows: `ipconfig`
   - Mac/Linux: `ifconfig`

3. **手机访问**
   - 在手机浏览器输入：`http://你的IP:5173`
   - 例如：`http://192.168.1.100:5173`

### 部署后测试

1. **在手机浏览器打开部署的 URL**
2. **测试功能**
   - ✅ 触摸交互是否流畅
   - ✅ 按钮是否容易点击
   - ✅ 文字是否清晰可读
   - ✅ 布局是否适配屏幕

## 🎯 移动端优化建议

### 已实现
- ✅ 响应式布局
- ✅ 触摸优化
- ✅ PWA 支持
- ✅ 安全区域适配

### 可进一步优化（可选）

1. **添加 Service Worker**（离线支持）
   ```bash
   npm install -D vite-plugin-pwa
   ```

2. **优化图片加载**
   - 使用 WebP 格式
   - 添加懒加载

3. **性能优化**
   - 代码分割
   - 压缩资源

## 🔧 常见问题

### Q: 手机上显示不正常？
A: 检查 viewport meta 标签是否正确，清除浏览器缓存

### Q: 无法添加到主屏幕？
A: 确保 manifest.json 正确配置，使用 HTTPS（本地 localhost 也可以）

### Q: 触摸不灵敏？
A: 检查按钮是否有足够的点击区域（至少 44x44px）

### Q: 横屏显示异常？
A: 已在 mobile.css 中添加横屏适配，如仍有问题可进一步调整

## 📝 下一步

1. 构建并部署项目
2. 在手机上测试
3. 根据实际使用情况进一步优化
